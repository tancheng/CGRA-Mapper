
dist: focal
language: ruby
compiler: gcc

install:
  - sudo apt-get update
  - sudo apt-get install llvm-12

script:
  - mkdir build && cd build && cmake ..
  - make
  - cd ../test && ./compile && ./run
